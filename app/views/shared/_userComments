 <div id="reviewComments" class="col-sm-4" >
	  	<div class="panel">
	  		<div class="panel-heading">
	  			<h3 class="panel-title">Your comments on <%= Review.find(@revId).title%></h3>
	  		</div>
	  			<div class="list-group">
	  			<% for comment in getRevComments(@user, @revId)%>
		  				<% if current_user == User.find(Review.find(comment.review_id).user_id)%>
		  				<%= link_to comment.text[0..10] + "...", root_path, { :class => "list-group-item", :id => "list-button" } %>
	  				<% end %>
	  			<% end %>
	  		</div>
	  			<%= link_to "See More", root_path, :class => "btn btn-success btn-block" %>
	  		</div>
	 	</div>	
</div>