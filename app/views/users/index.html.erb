<h1>This will eventually be a homepage</h1>
<h2>This is just for testing</h2>

<ul>
	<%for user in @users%>
	<li><%= link_to user.username, :action => "show", :id => user.id%>, <%=user.email%>, <%= user.id%>, <%=user.encrypted_password %><br>
		<%= button_to "Delete", :action => "deleteuser", :id => user.id%>
		<%= link_to "Edit", :action => "edit", :id => user.id%>
	</li>
	<%end%>
</ul>


